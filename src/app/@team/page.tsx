import type { NextPage } from 'next'
import type { ReactNode } from 'react'

interface Props {
  children?: ReactNode
}

const Team: NextPage<Props> = (props, initProps) => {
  return <>Team</>
}

export default Team
