import type { NextPage } from 'next'
import type { ReactNode } from 'react'

interface Props {
  children?: ReactNode
}

const Analytics: NextPage<Props> = (props, initProps) => {
  return <>Analytics</>
}

export default Analytics
